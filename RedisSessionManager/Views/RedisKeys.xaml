<UserControl x:Class="RedisSessionManager.Views.RedisKeys"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RedisSessionManager.Views"
             xmlns:viewModels="clr-namespace:RedisSessionManager.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="White">

    <Grid Margin="5">

        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0">

            <StackPanel Orientation="Horizontal">
                <StackPanel>
                    <TextBox Width="150" Height="25" />
                    <TextBlock FontSize="10" Text="Enter search pattern" />
                </StackPanel>
                <Button Margin="5,0,0,0" Width="60" Height="25"  VerticalAlignment="Top">Refresh</Button>
            </StackPanel>

        </StackPanel>

        <StackPanel Grid.Row="0" Grid.Column="1">

            <TextBlock Text="Total Sessions: 5" />
            <TextBlock Text="Number of Keys: 5" Margin="0,10,0,0" />
            <TextBlock Text="Total Size: 5" Margin="0,10,0,0" />

        </StackPanel>

        <ListView Grid.Row="1" Grid.ColumnSpan="2" ItemsSource="{Binding RedisData}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Key" DisplayMemberBinding="{Binding Key}" Width="150"/>
                    <GridViewColumn Header="Value" DisplayMemberBinding="{Binding Value}" Width="150"/>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>

</UserControl>
